{
  "name": "notes",
  "version": "1.0.0",
  "description": "Plain text notes app",
  "scripts": {
    "start": "node server/index.js",
    "build": "cd client && npm install && npm run build"
  },
  "author": "Ashley Terstriep"
}
